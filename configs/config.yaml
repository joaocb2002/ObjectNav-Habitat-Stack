# Hydra root config (composition)
# Compose defaults from config groups.
# CLI overrides take highest precedence.
# This is the “entry” config that picks an experiment by default.

defaults:
  - experiment: baseline
  - _self_
